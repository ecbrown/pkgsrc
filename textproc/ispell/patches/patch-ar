$NetBSD: patch-ar,v 1.2 2014/05/14 02:28:13 rodent Exp $

Remove an obsolete declaration that conflicts with the standard prototype.

Keep the hunk at its current release offset.  GNV patch's backup path loses
filename case when a hunk must be relocated on OpenVMS.

--- ijoin.c.orig
+++ ijoin.c
@@ -172,8 +172,6 @@
 static int		unpairable1 = 0; /* NZ if -a1 */
 static int		unpairable2 = 0; /* NZ if -a2 */
 
-extern int	strcmp ();
-
 int main (argc, argv)			/* Join files */
     int			argc;		/* Argument count */
     char *		argv[];		/* Argument vector */
