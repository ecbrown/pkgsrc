#!/bin/sh
# $NetBSD$

# Keep the dollar sign in the native VSI OpenSSL pathname out of command lines
# generated by pkgsrc and packages.  GNV's shell would otherwise expand
# "$common" as a shell parameter.
exec '/sys$common/ssl3/x86_64_exe/openssl.exe' "$@"
